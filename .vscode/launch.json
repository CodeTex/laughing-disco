{
  "configurations": [
    {
      "name": "Python: Run API server",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "src.api:app",
        "--host",
        "localhost",
        "--port",
        "8000",
        "--log-level",
        "debug",
        "--reload"
      ],
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}
